# ─────────────────────────────────────────────
# dabaeum
#
# 원칙
#   커밋한다  AGENTS.md · .codex/config.toml · docs/superpowers · docs/decisions
#             docs/reports · gradle/wrapper/*.jar
#             → 에이전트가 다음 세션에서 맥락을 되찾는 근거이자 팀 규약
#   무시한다  빌드 산출물 · 에이전트 스크래치 · 파생 문서 · 인증정보
# ─────────────────────────────────────────────

### Gradle · 빌드 ###
.gradle
build/
!gradle/wrapper/gradle-wrapper.jar
!**/src/main/**/build/
!**/src/test/**/build/
bin/
!**/src/main/**/bin/
!**/src/test/**/bin/
out/
!**/src/main/**/out/
!**/src/test/**/out/

### Spring Initializr 잔재 ###
HELP.md


# ─────────────────────────────────────────────
# 인증정보
# AGENTS.md "인증정보를 커밋하지 않는다"의 강제 장치.
# 비밀값은 저장소에 두지 않고 환경변수로만 주입한다.
# Jasypt를 쓰는 경우 ENC(...) 값과 master password 모두 환경변수로 준다.
# 이름이 아니라 확장자로 넓게 막는다 — 파일명은 언제든 달라진다.
# ─────────────────────────────────────────────
*.env
!*.env.example
!**/*.env.example
integration-test.env

# Fabric User1 인증서 · priv_sk · TLS CA · SSH 키
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.truststore
priv_sk
**/msp/**
id_rsa*
id_ed25519*
known_hosts


# ─────────────────────────────────────────────
# macOS + 외장 exFAT 볼륨
# build.gradle의 removeAppleDoubleFiles 훅과 짝을 이룬다.
# ._* 파일이 classpath 스캔을 깨뜨리므로, 커밋되면 다른 환경에서 빌드가 재현되지 않는다.
# ─────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems


# ─────────────────────────────────────────────
# 에이전트 작업 흔적
# 계획(docs/superpowers) · 설계(docs/specs) · 보고서(docs/reports) · ADR은 커밋 대상이다.
# 여기서 막는 것은 중간 산출물과 백업뿐이다.
# ─────────────────────────────────────────────
_backup/
_to_delete/
_scratch/
scratch/
tmp/
temp/
outputs/
output/
*.orig
*.bak
*.rej

# 에이전트 로컬 상태 — *.local.* 이 붙으면 개인 설정이라는 관례를 따른다.
# .codex/config.toml은 MCP 서버 규약이므로 커밋한다.
.codex/sessions/
.codex/*.local.toml
.claude/settings.json
.claude/settings.local.json
.ua/
.claude/sessions/
.superpowers/cache/
.aider*


# ─────────────────────────────────────────────
# 파생 산출물 — 원본에서 다시 만들 수 있는 것
# PDF는 docs/api/guide/index.html 의 인쇄 결과물(3.8MB)이다.
# 문서를 고칠 때마다 저장소가 그만큼 불어나므로 배포는 릴리스 아티팩트로 한다.
# ─────────────────────────────────────────────
docs/api/dabaeum-api-flow.pdf
build/generated-snippets/


### 로그 ###
*.log
logs/


### IDE ###
.idea
*.iws
*.iml
*.ipr
.vscode/
.apt_generated
.classpath
.factorypath
.project
.settings
.springBeans
.sts4-cache
/nbproject/private/
/nbbuild/
/dist/
/nbdist/
/.nb-gradle/


### Go — fabric/chaincode ###
fabric/**/vendor/
*.test


### Python 잔재 — Qwen MCP 브릿지(tools/) 제거 후 남은 캐시 ###
.pytest_cache/
__pycache__/
*.pyc
.venv/

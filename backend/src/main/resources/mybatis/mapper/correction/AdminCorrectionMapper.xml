<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.adn.dabaeum.correction.infrastructure.mybatis.AdminCorrectionMapper">

    <insert id="insert">
        INSERT INTO tb_admin_corrections (
            id, target_type, target_id, from_status, to_status,
            reason, corrected_by, corrected_at
        ) VALUES (
            #{id}, #{targetType}, #{targetId}, #{fromStatus}, #{toStatus},
            #{reason}, #{correctedBy}, #{correctedAt}
        )
    </insert>
</mapper>

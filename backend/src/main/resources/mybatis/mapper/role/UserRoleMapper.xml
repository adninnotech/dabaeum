<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.adn.dabaeum.role.infrastructure.mybatis.UserRoleMapper">

    <resultMap
        id="userRoleRowResultMap"
        type="com.adn.dabaeum.role.infrastructure.mybatis.UserRoleRow">
        <constructor>
            <idArg
                column="id"
                javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg
                column="user_id"
                javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg
                column="institution_id"
                javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg column="role" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO tb_user_roles (
            id,
            user_id,
            institution_id,
            role,
            created_at
        ) VALUES (
            #{id},
            #{userId},
            #{institutionId},
            #{role},
            #{createdAt}
        )
    </insert>

    <insert id="insertIfAbsent">
        INSERT INTO tb_user_roles (
            id, user_id, institution_id, role, created_at
        ) VALUES (
            #{id}, #{userId}, #{institutionId}, #{role}, #{createdAt}
        )
        ON CONFLICT DO NOTHING
    </insert>

    <select id="selectById" resultMap="userRoleRowResultMap">
        SELECT id, user_id, institution_id, role, created_at
          FROM tb_user_roles
         WHERE id = #{id}
    </select>

    <select id="selectByIdForUpdate" resultMap="userRoleRowResultMap">
        SELECT id, user_id, institution_id, role, created_at
          FROM tb_user_roles
         WHERE id = #{id}
         FOR UPDATE
    </select>

    <select id="selectByUserId" resultMap="userRoleRowResultMap">
        SELECT id, user_id, institution_id, role, created_at
          FROM tb_user_roles
         WHERE user_id = #{userId}
         ORDER BY created_at ASC, id ASC
    </select>

    <select
        id="selectByUserIdAndInstitution"
        resultMap="userRoleRowResultMap">
        SELECT id, user_id, institution_id, role, created_at
          FROM tb_user_roles
         WHERE user_id = #{userId}
           AND institution_id = #{institutionId}
         ORDER BY created_at ASC, id ASC
    </select>

    <select
        id="selectByUserIdAndInstitutionForUpdate"
        resultMap="userRoleRowResultMap">
        SELECT id, user_id, institution_id, role, created_at
          FROM tb_user_roles
         WHERE user_id = #{userId}
           AND institution_id = #{institutionId}
         ORDER BY created_at ASC, id ASC
         FOR UPDATE
    </select>

    <delete id="deleteByIdAndUserId">
        DELETE FROM tb_user_roles
         WHERE id = #{roleId}
           AND user_id = #{userId}
    </delete>

    <update id="updateInstructorMemo">
        UPDATE tb_user_roles
           SET memo = #{memo, jdbcType=VARCHAR}
         WHERE institution_id = #{institutionId}
           AND user_id = #{userId}
           AND role = 'INSTRUCTOR'
    </update>

</mapper>

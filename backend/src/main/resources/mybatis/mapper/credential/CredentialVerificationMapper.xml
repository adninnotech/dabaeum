<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.adn.dabaeum.credential.infrastructure.mybatis.CredentialVerificationMapper">

    <resultMap id="credentialVerificationRowResultMap"
        type="com.adn.dabaeum.credential.infrastructure.mybatis.CredentialVerificationRow">
        <constructor>
            <idArg column="id" javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg column="credential_id" javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg column="presented_credential_no" javaType="java.lang.String"/>
            <arg column="presented_hash" javaType="java.lang.String"/>
            <arg column="verification_type" javaType="java.lang.String"/>
            <arg column="requester_type" javaType="java.lang.String"/>
            <arg column="requester_id" javaType="java.lang.String"/>
            <arg column="result" javaType="java.lang.String"/>
            <arg column="verified_at" javaType="java.time.Instant"/>
            <arg column="request_ip" javaType="java.lang.String"/>
            <arg column="verification_hash" javaType="java.lang.String"/>
            <arg column="metadata" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO tb_credential_verifications (
            id, credential_id, presented_credential_no, presented_hash,
            verification_type, requester_type, requester_id, result, verified_at,
            request_ip, verification_hash, metadata, created_at
        ) VALUES (
            #{id}, #{credentialId, jdbcType=OTHER}, #{presentedCredentialNo, jdbcType=VARCHAR},
            #{presentedHash, jdbcType=VARCHAR}, #{verificationType}, #{requesterType},
            #{requesterId, jdbcType=VARCHAR}, #{result}, #{verifiedAt, jdbcType=TIMESTAMP_WITH_TIMEZONE},
            NULL, #{verificationHash, jdbcType=VARCHAR},
            CAST(#{metadata, jdbcType=VARCHAR} AS JSONB), #{createdAt, jdbcType=TIMESTAMP_WITH_TIMEZONE}
        )
    </insert>

    <select id="selectByCredentialId" resultMap="credentialVerificationRowResultMap">
        SELECT id, credential_id, presented_credential_no, presented_hash,
               verification_type, requester_type, requester_id, result, verified_at,
               request_ip, verification_hash, metadata::text AS metadata, created_at
          FROM tb_credential_verifications
         WHERE credential_id = #{credentialId}
         ORDER BY
        <choose>
            <when test="sortField == 'verified_at'">verified_at</when>
            <otherwise>created_at</otherwise>
        </choose>
        <choose>
            <when test="sortDirection == 'ASC'">ASC</when>
            <otherwise>DESC</otherwise>
        </choose>, id ASC
         LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByCredentialId" resultType="long">
        SELECT COUNT(*)
          FROM tb_credential_verifications
         WHERE credential_id = #{credentialId}
    </select>
</mapper>

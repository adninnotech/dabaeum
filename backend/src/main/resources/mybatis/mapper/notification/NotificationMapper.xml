<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.adn.dabaeum.notification.infrastructure.mybatis.NotificationMapper">

    <resultMap
        id="notificationRowResultMap"
        type="com.adn.dabaeum.notification.infrastructure.mybatis.NotificationRow">

        <constructor>
            <idArg
                column="id"
                javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg column="user_id" javaType="java.util.UUID"
                typeHandler="org.apache.ibatis.type.ObjectTypeHandler"/>
            <arg column="title" javaType="java.lang.String"/>
            <arg column="body" javaType="java.lang.String"/>
            <arg column="read_at" javaType="java.time.Instant"/>
            <arg column="created_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO tb_notifications (
            id, user_id, title, body, read_at, created_at
        ) VALUES (
            #{id}, #{userId}, #{title},
            #{body, jdbcType=VARCHAR},
            #{readAt, jdbcType=TIMESTAMP_WITH_TIMEZONE},
            #{createdAt}
        )
    </insert>

    <select id="selectById" resultMap="notificationRowResultMap">
        SELECT id, user_id, title, body, read_at, created_at
        FROM tb_notifications
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="notificationRowResultMap">
        SELECT id, user_id, title, body, read_at, created_at
        FROM tb_notifications
        WHERE user_id = #{userId}
        <if test="unreadOnly">
          AND read_at IS NULL
        </if>
        ORDER BY created_at DESC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(*)
        FROM tb_notifications
        WHERE user_id = #{userId}
        <if test="unreadOnly">
          AND read_at IS NULL
        </if>
    </select>

    <select id="countUnread" resultType="long">
        SELECT COUNT(*)
        FROM tb_notifications
        WHERE user_id = #{userId}
          AND read_at IS NULL
    </select>

    <update id="markRead">
        UPDATE tb_notifications
           SET read_at = #{readAt}
         WHERE id = #{id}
           AND user_id = #{userId}
           AND read_at IS NULL
    </update>

</mapper>
